a,b=map(int,input().split())

for i in range(a,b+1):
    x=i%10
    y=int((i%100)/10)
    z=int((i%1000)/100)
    w=int((i%10000)/1000)
    
    if x%2==1 and y%2==1 and z%2==1 and w%2==1:
        print(i,end=' ')
